// return statement

function subtract(num) {
    return num - 7;
}

console.log(subtract(8));

// functions don't have to have return statements
function add(sum) {
    console.log(sum + 3);
}

add(3);
